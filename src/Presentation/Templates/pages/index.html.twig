{% extends 'base.html.twig' %}
 
{% block body %}

<div class="container">
    <a href="{{ path('security_login')}}">Login</a>
    <a href="{{ path('user_registration')}}">Sign Up</a>
</div>
{% endblock %}